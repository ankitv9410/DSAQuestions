for (int i = 0; i < arr.length; i++) {
			int flag = 0;
			for (int j = 0; j < arr.length; j++) {
				if (arr[i] == arr[j])
					flag++;
			}
			if (flag == 1)
				return arr[i];
		}
		return -1;
    Time O(n^2) space O(1)
